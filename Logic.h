#ifndef LOGIC
#define LOGIC
#include "INTER_usr.h"
#include <string>
#include <map>

class Logic {
 
std::map<std::string, INTER_usr*> users;

std::map.insert({user1.name, &user1});

};

#endif